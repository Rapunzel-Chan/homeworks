[tool.poetry]
name = "pythonproject5"
version = "0.1.0"
description = "Программа для домашки"
authors = ["RapunzelChan <rapuncel.chan24@gmail.com>"]
readme = "README.md"
packages = [{ include = "gruyere_scapy_hw" }]

[tool.poetry.dependencies]
python = ">=3.11,<4.0"
scapy = ">=2.7.0,<3.0.0"
pandas = "^3.0.0"
matplotlib = "^3.10.8"
seaborn = "^0.13.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
